export enum TextElements {
  H1 = "h1",
  H2 = "h2",
  P = "p",
  BALANCE = "balance",
  CURRENCY = "currency",
}

export enum TextVariants {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  TERTIARY = "tertiary",
}
